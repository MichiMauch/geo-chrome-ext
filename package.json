{
  "name": "geo-analyzer-extension",
  "version": "3.2.0",
  "description": "Chrome Extension for GEO analysis of web pages with score tracking, trend analysis and history",
  "type": "module",
  "scripts": {
    "dev": "npm run build:popup && npm run build:content -- --watch",
    "build": "npm run build:popup && npm run build:content && npm run build:background && npm run build:report && node scripts/post-build.js",
    "build:popup": "vite build",
    "build:content": "BUILD_TARGET=content vite build",
    "build:background": "BUILD_TARGET=background vite build",
    "build:report": "BUILD_TARGET=report vite build",
    "type-check": "tsc --noEmit",
    "package": "npm run build && cd dist && zip -r ../geo-analyzer-extension.zip .",
    "test": "vitest"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@types/chrome": "^0.1.32",
    "autoprefixer": "^10.4.23",
    "jsdom": "^29.1.1",
    "postcss": "^8.5.6",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3",
    "vite": "^6.4.1",
    "vitest": "^4.1.2"
  }
}
